name: goreleaser

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  # Required for Sigstore keyless signing: goreleaser exchanges this token
  # at Fulcio for a short-lived signing certificate.
  id-token: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v6
      - name: Install UPX
        run: sudo apt-get update && sudo apt-get install -y upx
      - name: Install Cosign
        uses: sigstore/cosign-installer@v3
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v7
        with:
          # either 'goreleaser' (default) or 'goreleaser-pro'
          distribution: goreleaser
          # 'latest', 'nightly', or a semver
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
